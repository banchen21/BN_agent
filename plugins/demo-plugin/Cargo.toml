[package]
name = "demo-plugin"
version = "0.1.0"
edition = "2021"
description = "示例插件 — 订阅 'greeting' 事件并发布 'greeted' 响应"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-interface = { path = "../../plugin-interface" }
serde_json = "1"
log = "0.4"
