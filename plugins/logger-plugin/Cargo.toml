[package]
name = "logger-plugin"
version = "0.1.0"
edition = "2021"
description = "Example plugin — subscribes to all events ('*') and logs them"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-interface = { path = "../../plugin-interface" }
serde_json = "1"
log = "0.4"
