[package]
name = "tui-plugin"
version = "0.1.0"
edition = "2021"
description = "Terminal UI plugin — in-app chat interface with stdin/stdout"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-interface = { path = "../../plugin-interface" }
crossterm = "0.28"
serde_json = "1"
log = "0.4"
