[package]
name = "claude-bridge-plugin"
version = "0.1.0"
edition = "2021"
description = "Claude CLI bridge plugin"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-interface = { path = "../../plugin-interface" }
serde_json = "1"
log = "0.4"
