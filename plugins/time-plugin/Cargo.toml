[package]
name = "time-plugin"
version = "0.1.0"
edition = "2021"
description = "Time plugin — passive context (snapshot) + HTTP API"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-interface = { path = "../../plugin-interface" }
chrono = "0.4"
serde_json = "1"
log = "0.4"
